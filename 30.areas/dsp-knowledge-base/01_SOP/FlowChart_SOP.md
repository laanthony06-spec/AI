# FlowChart SOP

## 使用场景

当需求包含多条件判断、多系统交互、异常分支或循环逻辑时，应补充流程图。

## 推荐结构

```text
开始
↓
数据获取
↓
条件判断
├─ 是 → 执行逻辑 A
└─ 否 → 执行逻辑 B
↓
输出结果
↓
结束
```

## 要求

- 判断节点必须对应正文逻辑。
- 每个分支要有明确结论。
- 异常流程不能省略。
- 字段不确定时写 `【待确认】`。

